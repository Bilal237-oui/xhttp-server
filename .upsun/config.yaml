applications:
    app:
        type: nodejs:20
        disk: 2048
        size: "S"
        web:
            commands:
                start: node server.js
            locations:
                /:
                    passthru: true

routes:
    "https://{default}/":
        type: upstream
        upstream: "app:http"
    "http://{default}/":
        type: redirect
        to: "https://{default}/"
